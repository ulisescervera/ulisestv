package com.gmail.uli153.ulisestv.core

object Constants {
    /** Base URL of the backend. Replace with the real endpoint. */
    const val BASE_URL = "https://api.ulisestv.com/"

    /** Room database file name. */
    const val DATABASE_NAME = "ulisestv.db"

    /** Default network timeout in seconds. */
    const val NETWORK_TIMEOUT_SECONDS = 30L
}
