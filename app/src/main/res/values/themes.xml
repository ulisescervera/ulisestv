<resources>

    <!-- Compose drives the actual theming (see UlisesTvTheme); this only needs
         to be a NoActionBar platform theme so the window has no legacy ActionBar. -->
    <style name="Theme.UlisesTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
